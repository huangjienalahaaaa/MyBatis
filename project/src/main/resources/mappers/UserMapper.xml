<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.hj.mapper.UserMapper">

	<select id="findById" parameterType="int" resultType="User">
		select * from user where id = #{uid}
   	</select>



	<select id="findAllLazy" resultMap="userLazyMap">
		select * from user
	</select>


	<select id="userLazyMap" parameterType="int" resultType="account">
		select * from account where uid = #{uid}
	</select>


	<resultMap type="user" id="userLazyMap">
		<id column="id" property="id"/>
		<result column="username" property="username"/>
		<result column="birthday" property="birthday"/>
		<result column="sex" property="sex"/>
		<result column="addresss" property="addresss"/>


		<collection property="accounts" ofType="Account" select="cn.hj.mapper.AccountMapper.findByUid" column="id" fetchType="lazy">
			<id column="id" property="id"/>
			<result column="uid" property="uid"/>
			<result column="money" property="money"/>
		</collection>
	</resultMap>

</mapper>